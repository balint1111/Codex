{{- $values := .Values.backend | default .Values }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: codex-backend
  labels:
    app: codex-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codex-backend
  template:
    metadata:
      labels:
        app: codex-backend
    spec:
      containers:
        - name: backend
          image: {{ $values.image }}
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            {{- range $name, $value := $values.env }}
            - name: {{ $name }}
              value: "{{ $value }}"
            {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: codex-backend
  labels:
    app: codex-backend
spec:
  type: {{ $values.service.type }}
  selector:
    app: codex-backend
  ports:
    - port: 80
      targetPort: 8080
      {{- if $values.service.nodePort }}
      nodePort: {{ $values.service.nodePort }}
      {{- end }}
