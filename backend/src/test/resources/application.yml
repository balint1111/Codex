spring:
  liquibase:
    url: jdbc:postgresql://localhost:5432/codex
    user: codex
    password: codex
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/codex
    username: codex
    password: codex
    pool:
      max-size: 4
      initial-size: 0
      max-idle-time: 30s
  jooq:
    sql-dialect: POSTGRES
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: "http://keycloak-fake-set-uri"

frontendUrl: localhost:8080
logging:
  level:
    org.jooq.tools.LoggerListener: DEBUG
