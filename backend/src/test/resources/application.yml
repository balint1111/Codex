spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codex
    username: codex
    password: codex
  jooq:
    sql-dialect: POSTGRES
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: "http://keycloak-fake-set-uri"

frontendUrl: localhost:8080
logging.level.org.jooq.tools.LoggerListener: DEBUG
